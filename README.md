# Email-Application

This is a Email generator app for a mock company

the language uses was Java.
In the app this generates a email using the firstname n lastname of the user
It also generates you a random password for the user from a given set of characters
